CREATE TABLE user_profile (id INT AUTO_INCREMENT NOT NULL PRIMARY KEY, login VARCHAR(50) NOT NULL, passwd VARCHAR(50) NOT NULL, email VARCHAR(50) NOT NULL, rank INT DEFAULT 1000, UNIQUE KEY(LOGIN), UNIQUE KEY(EMAIL)) DEFAULT CHARSET=utf8;